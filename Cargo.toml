[package]
name = "http_trace"
version = "0.1.0"
authors = ["Billy Richardson <richardsondev>"]
edition = "2018"

[dependencies]
reqwest = { version = "0.12.9", features = ["json"] }
tokio = { version = "1", features = ["full"] }
url = "2"
